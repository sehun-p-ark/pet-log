<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="dev.dhkim.petlog.mappers.FeedMapper">
    <insert id="insertFeed" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `petlog`.`feed`
            (user_id, title, content, like_count, comment_count, created_at, updated_at)
        VALUES
            (#{userId}, #{title}, #{content}, #{likeCount}, #{commentCount}, #{createdAt}, null)
    </insert>
    <select id="selectFeedsForScroll" resultType="dev.dhkim.petlog.entities.feed.FeedEntity">
        SELECT `id` AS `id`,
               `user_id` AS `userId`,
               `title` AS `title`,
               `content` AS `content`,
               `like_count` AS `likeCount`,
               `comment_count` AS `commentCount`,
               `created_at` AS `createdAt`,
               `updated_at` AS `updatedAt`
        FROM `petlog`.`feed`
        <where>
            <if test = "lastFeedId != null">
                id &lt; #{lastFeedId}
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{size}
    </select>
</mapper>